import { IconsNameConstants } from './icons-name-constants';

describe('IconsNameConstants', () => {
  it('should create an instance', () => {
    expect(new IconsNameConstants()).toBeTruthy();
  });
});
